# TECHNICAL.md — 详细技术分析

测量日：2026-09-21（北京时间）。账号：Allegretto（¥199/月）。执行：Kimi K3（k3-agent）@ DeepSeek Harness。

## 1. 计费接口

两条 Connect-RPC（浏览器会话 Bearer token，`localStorage.access_token`，约 20 分钟过期，刷新页面重发）：

```
POST /apiv2/kimi.gateway.membership.v2.MembershipService/GetSubscriptionStats
POST /apiv2/kimi.gateway.membership.v2.MembershipService/ListBalanceActions
  body: {"page_size":20, "pageToken":"", "filter":{"unit":"UNIT_CREDIT","balance_types":["SUBSCRIPTION"]}}
```

- `GetSubscriptionStats` → `subscriptionBalance.amountUsedRatio`（官方计数器）、`kimiCodeUsedRatio`、`ratelimitCode5h/7d`
- `ListBalanceActions` → 逐条扣费流水，每条含 `timestamp`、`feature`（FEATURE_WORK / FEATURE_CODING）、`items[].amountRatio`
- 计费提交时点 = usage 时间戳（±3s 可配对，Code 通道 160/194 配对成功）

## 2. 锚点：1% ≡ ¥10

由 Code 控制变量实验（输出定价已知 ¥105/M）反推：长输出呼叫的扣费 ÷ 输出 token 数 × ¥105/M，六点交叉一致 → 1% ≡ API 标价 ¥10，全池 ≡ ¥1,000。（官方挂牌为美元 $3.00/$0.30/$15.00 per M，¥ 值按 1 USD ≈ ¥7.0 折算；汇率波动线性影响 ¥ 绝对值、不影响比值。隐藏思考占比的定量上界 k≤3.0% 给出锚点可行域 [¥963, ¥1,030]。）¥199/¥1,000 ≈ 5.03× 为**实测推算倍率**，非官方表述（官方档位口径为"Allegretto = Agent 档额度 4 倍"，系档位间相对倍数，不同口径）。

## 3. Code 通道费率卡

160 对 ±3s 配对中剔除 103 条 0.01% 保底后，57 对非保底 OLS，RMSE ¥0.0325 ≈ 量化噪声下限；Pout = ¥104.6/M（95% CI [101.1, 108.1]）与官方 ¥105 一致。`out` 字段本身已含 reasoning token（wire 回归 out ≈ 0.97×think + 0.65×visible，R²=0.999），三维拟合无需单列思考：

| 维度 | 实测 | 官方 API | 备注 |
|---|---:|---:|---|
| 未命中输入 | ¥13.3/M [13.2–13.4] | ¥21/M | 63 折 |
| 缓存命中 | **¥0.08/M [0.05–0.09]** | ¥2.1/M | 官方价 4%，非零 |
| 输出 | ¥105/M | ¥105/M | 持平 |

约束机制：0.01% 量化网格 + 单次保底 0.01%（= ¥0.10）。

## 4. Work 通道费率卡

14 个受控方程（同会话递进，缓存单调增长隔离 Pin/Pc/Pout），区间交集 + OLS：

| 维度 | 点解 | 量化带内区间 | 相对官方 |
|---|---:|---:|---|
| 未命中输入 | ¥37.2/M | [35.5, 38.9] | 1.7~1.85× |
| 缓存命中 | ¥1.82/M | [1.60, 2.10] | ≈官方缓存价 |
| 输出 | ¥269.6/M | [263, 272] | 2.5~2.6× |

OLS RMSE = ¥0.028 = 0.01% 网格的噪声下限（¥0.029），14/14 点在带内——拟合优度达该测量制度的物理极限。计费维度仅三项（未命中输入/缓存/输出），思考按惯例计入输出。

**回测**：用此卡预测上午 41.9 分钟任务账单，预测 ¥146.7 vs 流水实计 ¥145.7，偏差 +0.7%。

## 5. Work 通道的结构性费用

- **开机费**：每个新会话首轮即扣 ≈0.11%（≈¥1.10），对应 29K 系统提示词的未命中输入
- **隐藏调用**：标题生成等辅助调用计费但不在 wire.jsonl（约占 0.02%）
- **iFinD 插件**：317 次调用（309 次计费、8 次为 0）× 固定 0.02%/次 = 6.18%，按次抽成无界面提示
- **大粘贴文件化**：长文本粘贴转为文件引用 + 分块工具读取，不整体进入上下文

## 6. 流水与官方计数器的口径校准

995 条流水逐条合计 27.96%，同日官方计数器 25.35%——系统性高 +2.6%。成因：逐条 0.01% 量化 + 保底的累积膨胀（上午 644 条 +1.4%，下午 351 条 +1.2%，逐段吻合）。**相对分析用流水，池面水位以官方计数器为准。**

## 7. 上午任务解剖（官方计数器 ≈19.5%）

254 条去重调用、15.18M tokens（93.2% 缓存读）、41.9 分钟。成本构成（费率卡预测口径；同批 tokens 官方 API 标价 ¥83.0）：输出 ¥83.7（57%）/ 输入 ¥37.8（26%）/ 缓存 ¥25.2（17%）。同 tokens 走 Code 通道仅 ¥47.2——3.1 倍价差。

## 8. 购买力测算（实付口径）

¥199/月的有效标价算力：全走 Code ≈ ¥2,540；全走 Work ≈ ¥570；Work+iFinD 混合 ≈ ¥397。有效单价：Code ¥0.27/M vs Work ¥1.91/M（7×）。

## 9. 测量限制

单账户、单日、单地区；0.01% 网格 ⇒ 单点分辨率 ≈¥0.1；Code 缓存价分辨率受 256K 上下文封顶；计数器读数快照见 data/counter_snapshots.json；计费系统可能随时调整（官方新套餐体系传闻中，截至测量日未公示）。
