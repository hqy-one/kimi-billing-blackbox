# TECHNICAL.md — 技术附录：测量装置与推导

> 本文只回答「**怎么算的**」。结论与证据链见 [REPORT.md](./REPORT.md)（数值的唯一权威来源）；自行复现见 [METHOD.md](./METHOD.md)。

测量日：2026-09-21（北京时间）。账号：Allegretto（¥199/月）。执行：Kimi K3（k3-agent）@ DeepSeek Harness。

## 1. 计费接口（Connect-RPC，实测）

- 订阅快照：`POST /apiv2/kimi.gateway.membership.v2.MembershipService/GetSubscriptionStats` → `subscriptionBalance.amountUsedRatio` / `kimiCodeUsedRatio` / `ratelimitCode5h|7d`；
- 消费流水：`.../ListBalanceActions`，每页上限 20 条、`pageToken` 翻页、可按 `feature` 过滤；
- 流水实时追加（<1s），快照有数分钟聚合延迟；令牌约 20 分钟一签（Cookie 会话）；
- 前端佐证：订阅页只展示百分比与 5h/7d 窗口，无 token 数与费率。

## 2. 锚点推导（全池 = 10,000 credits）

官方 K3 API 挂牌价 $3.00/$0.30/$15.00 per M（输入未命中/缓存/输出；本文 ¥ 值按 1 USD ≈ ¥7.0 折算，汇率波动线性影响 ¥ 绝对值、不影响比值）。受控长输出调用：输出 6,618 + 输入 137 tokens，官方标价成本 ¥0.6978，网关实扣 0.0700%（= 7 credits；记账单位系本报告自定义：官方只给百分比，其流水最小粒度为 0.01%，故取全池 = 10,000 credits 使 1 credit = 0.01%）⇒ **1 credit ≡ ¥0.10，即 1% = 100 credits ≡ ¥10**。三条独立路径互证（单点 ¥996.8 / 聚合 OLS ¥992.8 / 比和 ¥997.0）；隐藏思考占比上界 k ≤ 3.0%（95%）⇒ 全池可行域 **[¥963, ¥1,030]**，即汇率 [¥0.0963, ¥0.1030]/credit。标尺维度是 Code 输出（实测价=官方价）；锚点调用输出占标价成本 99.6%，Code 输入折扣影响仅 0.16%。¥1,000 应读作购买力基准行（REPORT §5.4 首行），非池的现金价值。

## 3. Code 通道费率卡（方法）

20 个实验点（18 受控 + 2 有机）+ 160 对 ±3s 秒级配对（剔除 103 条 0.01% 保底后 57 对非保底）OLS 拟合 `billed = Pin·in + Pc·cache + Pout·out`：RMSE ¥0.0325 ≈ 量化噪声下限 ¥0.029；Pout = ¥104.6/M（95% CI [101.1, 108.1]）与官方 ¥105 一致。**`out` 字段已含 reasoning token**（wire 回归 `out ≈ 0.97×think + 0.65×visible`，R²=0.999；原始 API `completion_tokens ⊇ reasoning_tokens`），三维拟合无需单列思考。数值表见 REPORT §4.3。

## 4. Work 通道费率卡（方法）

下午受控会话（无插件、无 Swarm）：11 条指令 14 个模型步，wire `usage.record` 与流水按秒级 1:1 配对 → 14 方程解 3 未知数，区间取三条独立量化带交集；OLS RMSE ¥0.028（噪声下限），14/14 点落带。**回测**：预测上午任务 ¥146.7 vs 实扣 ¥145.7（+0.7%）。数值表见 REPORT §5.5。

## 5. iFinD 插件

317 次调用（309 次计费、8 次为 0）× 固定 0.02%/次 = 6.18%，与返回数据量无关；插件为 Moonshot 官方托管（v0.2.7），界面无按次扣费提示。

## 6. 流水与计数器的校准

995 条流水逐条合计 27.96%，官方计数器同日读数 25.35%，系统性 +2.6%（量化+保底累积膨胀；上午 644 条 +1.4%、下午 351 条 +1.2%，逐段吻合）。**规则：相对分析（费率卡、构成比、配对）用流水；绝对池面水位用官方计数器。** 计数器快照见 `data/counter_snapshots.json`。

## 7. 测量限制

单账户、单日（2026-09-21）、单地区；0.01% 网格 ⇒ 单点分辨率 ≈¥0.1；Code 缓存价分辨率受 256K 上下文封顶；¥ 绝对值依赖 1 USD ≈ ¥7.0 折算；计费系统可能随时调整（官方新套餐体系传闻中，截至测量日未公示）。
